using UnityEngine;

namespace Project.Scripts.Runtime.Vrm
{
    public class VRMAvatar : MonoBehaviour
    {
        [SerializeField] private GameObject avatar;
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
